<Page
    x:Class="DtfOrderAutomation.Pages.LastRunPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowDefinitions="Auto,*" Padding="8">

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
            <Button x:ConnectionId='4' x:Name="ClearBtn" Content="Clear"                       />
        </StackPanel>

        <!-- Log output -->
        <ScrollViewer x:ConnectionId='2' Grid.Row="1"
                      x:Name="LogScroll"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
            <TextBlock x:ConnectionId='3' x:Name="LogText"
                       FontFamily="Cascadia Code, Consolas, Courier New"
                       FontSize="12"
                       TextWrapping="NoWrap"
                       IsTextSelectionEnabled="True"
                       Padding="12"/>
        </ScrollViewer>

    </Grid>
</Page>

